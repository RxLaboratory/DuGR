function buildUI()
{
    var ui = DuScriptUI.scriptPanel( thisObj, true, true, new File($.fileName) );
    ui.addCommonSettings();

    // Build ui in ui.mainGroup
    // Add settings in ui.settingsGroup

    DuScriptUI.showUI(ui);
}
